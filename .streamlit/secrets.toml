# .streamlit/secrets.toml
# Streamlit reads this at runtime. Keep it out of version control.

##############################
# Database (used when source=auto or mysql)
##############################
[mysql]
host = "127.0.0.1"          # e.g. "localhost" or "db.myhost.com"
port = 3306                    # change if your DB uses a custom port
user = "root"
password = "cs394mysqlserverlogin"
database = "inflation_affordability_three"        # or your actual DB name

##############################
# API Keys (optional; app falls back to sample CSVs if unset)
##############################
BLS_KEY = "630f2ae88f4d4075b962e8c540c1e30a"           # https://data.bls.gov/registrationEngine/
CENSUS_KEY = "bf51b243339e7aeb60bcdf7de316507646d77e88"     # https://api.census.gov/data/key_signup.html

##############################
# (Optional) App flags
##############################
# Set to true to force MySQL even if not on 'auto'
# FORCE_MYSQL = "false"

